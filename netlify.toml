# Netlify configuration. Used both by `netlify dev` locally and by Netlify
# when you deploy. The chatbot function lives in netlify/functions/.

[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  command = "npm run dev"
  targetPort = 5173
